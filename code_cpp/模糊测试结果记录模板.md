# 模糊测试结果记录模板

## 测试执行信息

- **测试日期**: [填写日期]
- **测试工具**: AFL++ (版本: [填写版本])
- **运行环境**: Linux (Ubuntu/Debian)
- **目标程序**: `fuzz_bin/fuzz_target`

## 安装截图

### AFL++安装
[插入安装截图]
- 显示安装命令: `sudo apt-get install afl++`
- 显示安装成功信息
- 显示`afl-fuzz --help`输出

## 编译过程

### 编译命令
```bash
afl-g++ -std=c++17 -O2 -I./src -o fuzz_bin/fuzz_target \
    fuzz_test_target.cpp src/*.cpp
```

### 编译截图
[插入编译截图]
- 显示编译命令
- 显示编译成功信息

## 运行过程

### 运行命令
```bash
afl-fuzz -i fuzz_input -o fuzz_output -- ./fuzz_bin/fuzz_target
```

### 运行截图
[插入运行截图]
- 显示AFL++运行界面
- 显示实时统计信息
- 显示运行时间

## 测试结果

### 运行统计

从`fuzz_output/default/fuzzer_stats`文件获取：

```
start_time        : [时间戳]
last_update       : [时间戳]
run_time          : [运行时间，秒]
cycles_done       : [完成的周期数]
execs_done        : [总执行次数]
execs_per_sec     : [每秒执行次数]
paths_total       : [总路径数]
paths_favored     : [偏好路径数]
paths_found       : [发现的路径数]
paths_imported    : [导入的路径数]
max_depth         : [最大深度]
cur_path          : [当前路径]
pending_favs      : [待处理的偏好]
pending_total     : [待处理总数]
variable_paths    : [可变路径数]
stability         : [稳定性]
bit_flips         : [位翻转次数]
byte_flips        : [字节翻转次数]
arith             : [算术操作次数]
known_int         : [已知整数次数]
known_float       : [已知浮点数次数]
dictionary        : [字典使用次数]
havoc             : [破坏操作次数]
```

### 崩溃用例（如果发现）

#### 崩溃文件列表
```
fuzz_output/default/crashes/
├── id:000000,sig:11,src:000000,op:flip1,pos:0
├── id:000001,sig:11,src:000001,op:flip2,pos:1
└── ...
```

#### 崩溃文件内容
[插入崩溃文件内容截图]

#### 复现崩溃
[插入复现崩溃的截图]
- 显示崩溃命令
- 显示崩溃输出（段错误等）

### 未发现崩溃（如果未发现）

#### 运行时间证明
[插入运行时间截图]
- 显示运行时间至少5小时
- 显示`fuzzer_stats`文件内容

## 分析结果

### 发现的崩溃分析
[如果发现崩溃，分析崩溃原因]

### 测试覆盖率
[分析测试覆盖的代码路径]

### 测试有效性
[评估模糊测试的有效性]

